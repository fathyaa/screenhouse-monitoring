{
  "name": "monitoring-service",
  "version": "1.0.0",
  "description": "MQTT ingest, sensor data, alerts",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "simulate": "node scripts/simulate-sensors.js",
    "simulate:once": "node scripts/simulate-sensors.js --once",
    "simulate:mqtt": "node scripts/simulate-mqtt.js",
    "simulate:mqtt:once": "node scripts/simulate-mqtt.js --once",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "amqplib": "^0.10.9",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mqtt": "^5.15.1",
    "pg": "^8.20.0",
    "redis": "^5.12.1",
    "socket.io": "^4.8.1"
  }
}
